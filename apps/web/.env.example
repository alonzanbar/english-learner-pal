# Base URL of the word-list API. Omit for local dev when the backend runs locally (Vite proxies /api).
# For local dev against staging: use your staging Cloud Run URL (e.g. from terraform output backend_url).
# VITE_API_URL=https://wordlist-backend-staging-XXXXX.run.app
